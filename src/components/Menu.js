const Menu = (menuDetails) =>{

    return (
        <div>
            <h2>Menu</h2>
            {/* {menuDetails.map((menu)=>(
                <div className="abc"></div>
            ))} */}
        </div>
    )

}

export default Menu